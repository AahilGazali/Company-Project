rules_version = '2';

// Development rules - allow full access to all storage
// TODO: Implement proper security rules in production
service firebase.storage {
  match /b/{bucket}/o {
    // Allow full access to all files and directories
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
} 